To run the app you need to execute the following commands:

set FLASK_ENV=development
flask run app

example json body for postman: 
tool for encoding: https://base64.guru/converter/encode/image
{
  "images": [
    "yor encoded based64 string for the image"
  ]
}

ssd_mobilenet its the pretrained model.
mscoco_label - pbtxt file based on what the model does classification


